# You're set up — now make it truly yours 🛡️

You've just walked through everything Dusk Office can do. Here's your reference card for the road ahead.

## Fine-tune behavior

Open settings (`Cmd/Ctrl + ,`) and search **"Dusk Office"**:

| Setting | What it controls |
|---|---|
| `applyFavoriteOnStartup` | Auto-load your favorite variant when the editor starts |
| `rememberWorkspaceTheme` | Each project remembers its own theme |
| `autoSwitch.*` | Day/night light ↔ dark switching schedule |
| `adaptiveFocus.*` | Language + time-aware automatic variant selection |
| `workspaceFingerprint.enabled` | Auto-suggest a variant when you open a new project |
| `titleBar.alignWithTheme` | Sync the title bar color with the active theme |
| `statusBar.enabled` | Show or hide the Control Center button |

## Safety net — reset anytime

Run **`Dusk Office: Reset All Settings`** from the Command Palette. One command to:

- Restore VS Code default theme, product icons, and activity bar position
- Clear all Dusk Office preferences and workspace-specific values
- Leave **zero residue** — your editor returns to exactly how it was before

## Our promise to you

| | |
|---|---|
| **No telemetry** | No analytics, no remote calls, no tracking — ever |
| **100% local** | Every feature runs inside your VS Code instance |
| **No companion installs** | We will never ask you to install another extension |
| **GPL v3** | Fork it, audit it, contribute back |

## Join the conversation

Found a bug? Want a new variant? The build pipeline generates a new theme in ~10 minutes — from source palette to WCAG-validated output.

**[Open an issue on GitHub](https://github.com/SIDIKICONDE/dusk-office)** — every request is read.

## Also from the same developer

**🛠️ [NythyCleaner](https://nythycleaner.cloud)** — Your Mac, clean and fast. Desk and dev workflow in one tool.

| | |
|---|---|
| **Xcode cleanup** | 22 categories: DerivedData, archives, all simulator platforms, SwiftUI Previews, CocoaPods, Carthage, provisioning profiles, old runtimes — charts before every delete |
| **Full monitoring** | Live charts for CPU, memory, GPU, Neural Engine, network, disk I/O, processes, and thermals — with menu bar gauges and WidgetKit widgets |
| **Security audit** | Scan dev projects for known CVEs across npm, pip, Go, Deno, CocoaPods, Maven, Composer, and Bundler |
| **Dev dependencies** | 20 ecosystems scanned: Node, Python, Rust, Go, Flutter, Ruby, .NET, PHP, Swift, and more — detect outdated packages, update in bulk |
| **Disk treemap** | Hierarchical volume map, large-file scans, iCloud Drive treemap with evict/download actions |
| **Exact duplicates** | Byte-for-byte SHA-256 matching — true duplicates even when names or dates differ |
| **Privacy & maintenance** | TCC permissions audit, secure delete (DoD), DNS flush, Spotlight repair, scheduled cleanup |

The essentials are free. Go Pro to unlock everything.

If Dusk Office makes your editor feel right, NythyCleaner makes your Mac feel right.

**[Try NythyCleaner →](https://nythycleaner.cloud)**

---

Welcome to Dusk Office. Happy coding 🌒
